ByteArrayOutputStream buffer = scholarshipService.getWinnerAcceptPdf(operator_id, scholarship_pool,isAdmin);
				response.setContentType("application/pdf");
				response.setContentLength(buffer.size());
				ServletOutputStream out = response.getOutputStream();
				buffer.writeTo(out);
				out.flush();
				out.close();
				buffer.close();
