 if(scale === 'page-width')
                {
                    var page_width = document.getElementById('viewer').clientWidth / pdfPage.getViewport({ scale: 1 }).width
                    viewport = pdfPage.getViewport({ scale: page_width });
                }
                else
                {
                    viewport = pdfPage.getViewport({ scale: scale/100 });
                }
