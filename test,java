while(bufferedInputStream.read(bytes) != -1) {
        	Encoder encoder = DefaultEncoder.getInstance();
        	byte[] encodedBytes = encoder.encodeForHTML(bytes);
			arrayOutputStream.write(bytes);
        }
The method encodeForHTML(String) in the type Encoder is not applicable for the arguments (byte[])
